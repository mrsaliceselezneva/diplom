import Controller from "./Controller";

function Connector({ setSelectAdd, title }) {

    return (
        <Controller 
            setSelectAdd={setSelectAdd}
            title={title}
        />
    );
}

export default Connector;