import View from "./View";

function Controller({ setSelectAdd, title }) {

    return (
        <View 
            setSelectAdd={setSelectAdd}
            title={title}
        />
    );
}

export default Controller;
